#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,rem;
    scanf("%d",&n);
    rem=n%10;
    if(rem>=5){
        n=n+(10-rem);
    }
    else{
        n=n-rem;
    }
    printf("%d",n);
    return 0;
}
